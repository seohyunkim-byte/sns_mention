[pytest]
testpaths = tests
markers =
    integration: requires real Claude API key, set RUN_INTEGRATION=1
addopts = -ra --strict-markers
filterwarnings =
    ignore:Field name "register" in "Voice" shadows an attribute in parent "BaseModel":UserWarning
